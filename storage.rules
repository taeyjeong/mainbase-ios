rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{projectId}/photos/{fileName} {
      allow read, write: if request.auth != null;
    }

    match /operations/{itemId}/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
